import React from "react";

const Loader = () => {
  return (
    <div
      style={{
        display: "flex",
        minHeight: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Загрузка...
    </div>
  );
};

export default Loader;
